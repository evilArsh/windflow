import { useLog } from "@main/hooks/useLog"

export const MCPServiceId = "MCPService"
export const log = useLog(MCPServiceId)
export const mcpName = "windflow-mcp-client"
export const mcpVersion = "v0.0.1"
